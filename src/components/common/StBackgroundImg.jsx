import styled from 'styled-components'
import loginBackground from "../../src_assets/loginBackground.png"

const StBackgroundImg = styled.div`
  background: url(${loginBackground}) center center fixed;
  width: 100%;
  height: 100vh;
  background-position: top center;
  background-size: cover;
`

export default StBackgroundImg
